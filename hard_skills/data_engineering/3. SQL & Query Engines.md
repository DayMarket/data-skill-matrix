# 3. SQL & Query Engines

Этот трек посвящён навыкам работы с SQL, включая написание сложных запросов, оптимизацию производительности и эффективное использование ClickHouse и Trino.

## DE1 (Junior)
- Я пишу SELECT с фильтрацией, агрегацией и JOIN, CTE, window functions и аналитическими функциями.
- Я понимаю основные типы данных, первичные/внешние ключи и разницу OLTP vs OLAP.
- Я создаю таблицы и различаю физический дизайн: ORDER BY/primary key/data-skipping indexes в ClickHouse и partitioning/table properties Iceberg через Trino.
- Я понимаю уровни изоляции транзакций и нормальные формы (1NF–3NF).
- Я выполняю распределённые запросы через несколько источников, используя существующие каталоги Trino.

## DE2 (Middle)
- Я создаю MergeTree-таблицы, задаю primary key, partition key, TTL и понимаю процесс merges в ClickHouse.
- Я использую Trino как слой федерации: соединяю данные из S3 + ClickHouse, делаю CTAS или INSERT INTO в Iceberg.
- Я использую существующие каталоги Trino, формулирую требования и валидирую подключение нового источника; rollout catalog configuration выполняется совместно с platform/infra owners.
- Я читаю system-таблицы и query logs для анализа «узких мест» принадлежащих команде запросов и таблиц (дисковое I/O, merges, пробки по памяти).

## DE3 (Middle+)
- Я оптимизирую схемы (LowCardinality, Sparse columns), создаю Materialized Views и Replacing/Collapsing/Aggregating MergeTree; настраиваю TTL-политику и агрегирующие витрины в ClickHouse.
- Я по метрикам и логам диагностирую типовые проблемы принадлежащих команде query workloads, включая disk-bound merge, spill-to-disk и out-of-memory, и проверяю результат исправления.
- Я понимаю влияние типов данных и преобразований на производительность, избегаю неявных кастов.
- Я оптимизирую миграцию запросов с ClickHouse на Trino/Iceberg с учётом различий в синтаксисе и семантике.

## DE4 (Senior)
- Я проектирую физические модели хранения под SLA запросов (hot/cold слои, сортировка, партиционирование).
- Я анализирую cost-based plan Trino, использую session properties и Iceberg connector для оптимизации запросов.
- Я веду диагностику неоднозначных и кросс-системных проблем query workloads, подтверждаю root cause планами запросов и метриками и внедряю системные меры предотвращения.
- Я отслеживаю стоимость запросов и хранения данных; выявляю «дорогие» запросы и таблицы, предлагаю и реализую оптимизации с измеримым снижением затрат.
- Я провожу post-mortem анализ data-инцидентов и обучаю команду оптимизации запросов.

### Антипаттерны DE4
- ❌ Создаю Materialized Views без анализа их влияния на производительность записи и дисковое пространство.
- ❌ Не документирую архитектурные решения по схемам хранения.
- ❌ Реактивно разбираюсь с проблемами query workloads вместо проактивного мониторинга.

## DE5 (Staff)
- Я оцениваю влияние новых версий ClickHouse/Trino на data workloads и контракты, совместно с platform owners формирую план миграции и валидирую совместимость.
- Я формирую стратегию использования query engines в компании: стандарты, capacity planning, FinOps и governance каталога данных.
- Я определяю критерии оценки и провожу R&D/PoC движков для решений, затрагивающих несколько доменов, документируя ROI, adoption и exit plan.
- Я обучаю продуктовые команды оптимизации затрат на хранение и вычисления и провожу архитектурные ревью.
- Я определяю подход к автоматизированным миграциям схем и версионированию данных на уровне data-функции, включая compatibility, deprecation и rollback.
